services:
  db:
    image: postgres:18.1-alpine
    container_name: next-pizza_database
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: next-pizza
    ports:
      - "5432:5432"
    volumes:
      - storage:/var/lib/postgresql/data

volumes:
  storage:
